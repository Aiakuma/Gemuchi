<header>
    <nav class="purpleGrey navbar fixed-top navbar-expand-md">
        <div class="container-lg gap-md-5">
            <div class="logo">
                <a href="{{ path('home') }}" class="navbar-brand mb-0 h1">
                    <img src="{{ asset('/images/Logo_Japon.svg')}}" alt="logo" class="d-inline-block align-top" width="50" height="50">
                </a> 
            </div> 
            <div class="pseudo text-white text-center d-flex d-md-none t_Flood">Gemuchi</div>
            <div class="d-flex align-items-center">
                <form class="form-search w-auto">
                <div class="input-group">
                    <input type="search" placeholder="Rechercher" class="form-control  d-md-flex d-none">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-link"><i class="fas fa-search"></i></button>
                    </div>
                </div> 
            </form>
            
            <button type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" class="navbar-toggler d-flex flex-column justify-content-around  d-md-none collapsed"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation"
            >
                <span class="toggler-icon top-bar"></span>
                <span class="toggler-icon middle-bar"></span>
                <span class="toggler-icon bottom-bar"></span>
            </button>
            </div>
            
            
            <div class="collapse navbar-collapse"  id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item text-white">
                        <a href="{{ path('home') }}" class="nav-link">
                            Accueil
                        </a>
                    </li>
                    <li class="nav-item text-white">
                        <a href="{{ path('about') }}" class="nav-link">
                            A propos
                        </a>
                    </li>
                    <li class="nav-item text-white">
                        <a href="{{ path('index_tests') }}" class="nav-link">
                            Tests
                        </a>
                    </li>
                    <li class="nav-item text-white">
                        <a href="{{ path('contact') }}" class="nav-link">
                            Contact
                        </a>
                    </li>
                </ul>
            </div> 
        </div>
    </nav>
</header>

    



